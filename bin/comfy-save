#!/bin/bash
set -euo pipefail
cd "${COMFY_DIR:-/workspace/ComfyUI}"
git init >/dev/null 2>&1 || true
git add -A
git commit -m "local WIP $(date +%F_%T)" || echo "nothing to commit"
echo "Snapshot saved."
